<h3>Status</h3>


<table class="table table-hover text-left">
    <thead>
        <tr>
            <th>Usuário</th>
            <th>Status</th>
            <th>Base</th>
            <th>Data</th>
        </tr>
    </thead>
    <tbody>
        <% @atendimentos.each do |atendimento_chamado| %>
        <tr>
            <th><%= atendimento_chamado.user.nome %></th>
            <th><%= atendimento_chamado.status.nome %></th>
            <th><%= atendimento_chamado.base.nome %></th>
            <th><%= atendimento_chamado.data.to_s(:custom_datetime) %></th>
        </tr>
        <% end %>
    </tbody>
</table>

<% if @atendimentos.count > 10 %>
    <%= render :partial => 'shared/pagination', locals: { collection: @atendimentos } %>
<% end %>


<br>
<div class="actions d-flex justify-content-center">
    <%= link_to t('labels.new') << ' Status', new_atendimento_chamado_path(:chamado => @chamado.id), class: "btn btn-grad bg-grad" %>
</div>