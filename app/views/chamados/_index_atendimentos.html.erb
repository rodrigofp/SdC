<h3>Status</h3>
<p id="notice"><%= notice %></p>

<table class="table table-hover text-left">
    <thead>
        <tr>
            <th>Usuário</th>
            <th>Status</th>
            <th>Base</th>
            <th>Data</th>
            <th colspan="3"></th>
        </tr>
    </thead>
    <tbody>
        <% @atendimentos.each do |atendimento_chamado| %>
        <tr>
            <th><%= atendimento_chamado.usuario.nome %></th>
            <th><%= atendimento_chamado.status.nome %></th>
            <th><%= atendimento_chamado.base.nome %></th>
            <th><%= atendimento_chamado.data %></th>
            <%= render :partial => 'shared/button_index', locals: { show_path: atendimento_chamado, edit_path: edit_atendimento_chamado_path(atendimento_chamado, :chamado => @chamado.id) } %>
        </tr>
        <% end %>
    </tbody>
</table>
<div class="apple_pagination d-flex justify-content-center">
<%= will_paginate @atendimentos %>
</div>

<br>
<div class="actions d-flex justify-content-center">
    <%= link_to t('labels.new') << ' Status', new_atendimento_chamado_path(:chamado => @chamado.id), class: "btn btn-grad bg-grad" %>
</div>