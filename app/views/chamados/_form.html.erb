<%= form_with(model: chamado, local: true) do |form| %>
  <% if chamado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chamado.errors.count, "error") %> prohibited this chamado from being saved:</h2>

      <ul>
      <% chamado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :usuario_id %>
    <%= form.text_field :usuario_id %>
  </div>

  <div class="field">
    <%= form.label :data_abertura %>
    <%= form.datetime_select :data_abertura %>
  </div>

  <div class="field">
    <%= form.label :data_fechamento %>
    <%= form.datetime_select :data_fechamento %>
  </div>

  <div class="field">
    <%= form.label :modulo_id %>
    <%= form.text_field :modulo_id %>
  </div>

  <div class="field">
    <%= form.label :tipo_chamado_id %>
    <%= form.text_field :tipo_chamado_id %>
  </div>

  <div class="field">
    <%= form.label :prioridade_id %>
    <%= form.text_field :prioridade_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
