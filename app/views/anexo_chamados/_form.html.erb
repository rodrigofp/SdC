<%= form_with(model: anexo_chamado, local: true) do |form| %>
  <% if anexo_chamado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(anexo_chamado.errors.count, "error") %> prohibited this anexo_chamado from being saved:</h2>

      <ul>
      <% anexo_chamado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome_arquivo %>
    <%= form.text_field :nome_arquivo %>
  </div>

  <div class="field">
    <%= form.label :atendimento_chamado_status_id %>
    <%= form.text_field :atendimento_chamado_status_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
