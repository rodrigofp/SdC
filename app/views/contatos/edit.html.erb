<h1 class="card-header bg-grad">Alterar Contato</h1>

<%= render 'form', contato: @contato %>


<%= link_to 'Voltar', edit_usuario_path(@usuario_all.first), class: "btn btn-bottom btn-sm btn-voltar bg-voltar" %>
