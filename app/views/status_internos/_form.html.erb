<%= form_with(model: status_interno, local: true) do |form| %>
  <% if status_interno.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(status_interno.errors.count, "error") %> prohibited this status_interno from being saved:</h2>

      <ul>
      <% status_interno.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
