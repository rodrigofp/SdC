<%= form_with(model: status_externo, local: true) do |form| %>
  <% if status_externo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(status_externo.errors.count, "error") %> prohibited this status_externo from being saved:</h2>

      <ul>
      <% status_externo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
